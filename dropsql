if [ "$DJANGO_SETTINGS_MODULE" = brokerage.settings.unittest ]
then
  echo Removing db.sqlite3 file
  rm -f db.sqlite3
else
  psql -U postgres -h localhost -c "drop database \"brokerage-master-database\""
fi
